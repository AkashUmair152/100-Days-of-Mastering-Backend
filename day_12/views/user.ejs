<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Your Profile</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #121212;
      color: #e0e0e0;
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      align-items: center;
      min-height: 100vh;
    }

    .container {
      background-color: #1f1f1f;
      padding: 40px;
      border-radius: 12px;
      box-shadow: 0 6px 20px rgba(0, 0, 0, 0.5);
      width: 90%;
      max-width: 500px;
      margin: 30px auto;
    }

    h1 {
      color: #bb86fc;
      text-align: center;
      margin-bottom: 10px;
    }

    .profile-info {
      margin: 20px 0;
      line-height: 2;
    }

    .profile-info strong {
      color: #e0e0e0;
      display: block;
      margin-bottom: 10px;
    }

    .profile-info span {
      color: #03dac6;
      font-weight: 500;
      font-size: 1.1rem;
    }

    .actions {
      text-align: center;
      margin-top: 30px;
    }

    .btn {
      padding: 12px 20px;
      font-size: 16px;
      font-weight: bold;
      text-decoration: none;
      border-radius: 6px;
      display: inline-block;
      margin: 0 10px;
    }

    .btn-primary {
      background-color: #bb86fc;
      color: #121212;
      border: none;
    }

    .btn-primary:hover {
      background-color: #9966cc;
    }

    .btn-outline {
      background-color: transparent;
      color: #bb86fc;
      border: 2px solid #bb86fc;
    }

    .btn-outline:hover {
      background-color: #bb86fc;
      color: #121212;
    }

    .logout-form {
      display: inline;
    }

    .back-home {
      text-align: center;
      margin-top: 20px;
    }

    .back-home a {
      color: #b0b0b0;
      font-size: 0.9rem;
    }

    .back-home a:hover {
      color: #bb86fc;
    }
  </style>
</head>
<body>
  <div class="container">
    <!-- Dynamic Welcome Message -->
    <h1>Welcome, <span style="color:#03dac6;"><%= userName %></span>!</h1>
    <p style="text-align:center; color:#b0b0b0;">Here is your account information</p>

    <!-- User Info Display -->
    <div class="profile-info">
      <strong>Username:</strong> <span><%=userName %></span>
      <strong>Email:</strong> <span><%= email %></span>
      <strong>Age:</strong> <span><%= age || "Not provided" %></span>
    </div>

    <!-- Action Buttons -->
    <div class="actions">
      <a href="/edit" class="btn btn-outline">Edit Profile</a>
      <form action="/logout" method="POST" class="logout-form" onsubmit="return confirm('Are you sure you want to log out?');">
        <button type="submit" class="btn btn-primary">Logout</button>
      </form>
    </div>

    <!-- Back to Home -->
    <div class="back-home">
      <a href="/">← Back to Home</a>
    </div>
  </div>
</body>
</html>